[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mineiq"
version = "0.1.0"
description = "Real-Time Mineral Intelligence for Flotation Optimisation"
